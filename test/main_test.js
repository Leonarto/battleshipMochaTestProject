const expect = require('chai').expect;

// Test Suite, a block of unit tests, testing the same piece of code or function.
describe('Mocha', () => {
  // Test spec (a unit test)
  it('Should run our test using npm', () => {
    expect(true).to.be.ok;
  });
});